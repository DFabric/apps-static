deps:
  static:
  - libxml2-dev
  alpine:
  - g++
  - libgcrypt-dev
  - libgpg-error-dev
  - libressl
  - make
  - zlib-dev

version:
  src: https://git.gnome.org/browse/libxslt/refs/
  regex: (?<=>v).*(?=</a><)
